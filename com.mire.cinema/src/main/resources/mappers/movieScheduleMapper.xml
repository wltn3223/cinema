<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mire.cinema.repository.CinemaMapper">
	
	<!-- 스케줄 리스트 -->
	<select id="scheduleSelect" resultType="MovieSchedule">
		select * from movie_schedule
	</select>
	<!-- 스케줄 등록 -->
	<insert id="scheduleInsert">
		insert into movie_schedule(schedule_no, schedule_startTime, schedule_finishTime, screen_totalseat, screen_restSeat, movie_no, screen_no) 
		values(movie_schedule_seq.nextval,TO_DATE(#{scheduleStartTime}, 'YYYY-MM-DD HH24:MI'),TO_DATE(#{schduleFinishTime}, 'YYYY-MM-DD HH24:MI'),#{screenTotalSeat},#{screenRestSeat},#{movieNo},#{screenNo}); 
	</insert>
	<!-- 스케줄 수정 -->
	<update id="scheduleUpdate">
		update from movie_schedule schedule_startTime = #{scheduleStartTime},schedule_finishTime = #{scheduleFinishTime} where schedule_no = #{scheduleNo}
	</update>
	<!-- 스케줄 삭제 -->
	<delete id="scheduleDelete">
		delete from movie_schedule where schedule_no = #{scheduleNo}
	</delete>
	
</mapper>